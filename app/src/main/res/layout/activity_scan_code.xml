<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="@dimen/full_padding">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/full.padding"
            android:layout_marginStart="@dimen/full.padding"
            android:layout_toLeftOf="@+id/btn_flash"
            android:layout_toStartOf="@+id/btn_flash">

            <Button
                android:id="@+id/btn_back"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/btn.back" />
        </FrameLayout>

        <Button
            android:id="@+id/btn_flash"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_marginEnd="@dimen/full.padding"
            android:layout_marginRight="@dimen/full.padding"
            android:drawableLeft="@drawable/ic_light_off"
            android:drawablePadding="@dimen/half.padding"
            android:drawableStart="@drawable/ic_light_off"
            android:text="@string/btn.light" />
    </RelativeLayout>
</RelativeLayout>